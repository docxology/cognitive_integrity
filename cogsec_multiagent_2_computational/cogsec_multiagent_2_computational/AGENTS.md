# cogsec_multiagent_2_computational/ (nested duplicate) — Agent Notes

**Stale nested duplicate path**, not a package. Contains only an empty
`output/data/` directory tree. The real project is the parent directory
(`../../`). Likely a leftover from an earlier checkout or mis-scoped pipeline
run; retained for inspection only. Do not add code or imports here; do not
treat `cogsec_multiagent_2_computational/output/data` as project output — the
authoritative outputs live in `../../output/` and
`../../output/data_publication/`.
