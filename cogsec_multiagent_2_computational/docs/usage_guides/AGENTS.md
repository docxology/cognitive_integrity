Per-module usage guides (01_cognitive_firewall … 09_redteam_evaluation, one
markdown per defense/eval module). Keep each guide in sync with its `src/`
module; usage-guide drift is checked in review, not by a machine gate here.