Manuscript tooling for Part 2: `claim_registry.py` (claim→evidence registry),
`figure_registry.py`, `injector.py` (token injection), `latex_converter.py`,
`verifier.py` (the manuscript gate used by CI). Every published number must
resolve through the claim registry to a shipped artifact
(`output/data_publication/simulation/full_evaluation_results.json` or
`output/data/`).