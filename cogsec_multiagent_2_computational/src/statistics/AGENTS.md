Statistical-analysis modules: ANOVA, Bayesian, nonparametric, regression,
effect size, confidence intervals, cross-validation, hypothesis testing,
sensitivity/stability analysis, assumption checks, and `analysis_runner.py`
orchestrating them. Real data only — numbers must derive from `output/`/
`output/data_publication/` artifacts.