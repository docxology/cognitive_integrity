Table-generation modules for Part 2: ablation, assumption, corpus,
cross-validation, detection, scalability, stability, and statistical tables,
plus `latex.py` (LaTeX table rendering) and `binomial_ci.py` (confidence
intervals for corpus proportions).