# Generated verification logs — regenerated by scripts/verify_manuscript.py on
# every run. These were previously git-tracked and opened in append mode at
# module-import time, so `make verify` dirtied the working tree and the file
# grew unbounded (408 KB when untracked, 2026-07-27).
manuscript_verification.log
output/logs/
