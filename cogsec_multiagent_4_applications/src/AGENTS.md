# Agent Instructions: Source Code

Agents modifying the source code must adhere to:

- **No Mocks Policy:** Always use real methods. Never use mock or fake methods.
- **Modularity:** Keep functions small, focused, and pure where possible.
- **Logging:** Ensure functions are adequately logged.
- All code changes must be run against the test suite (`tests/`).
