# Agent Instructions: Output

Agents should:

- Monitor the `output/logs/` or terminal output when diagnosing build failures.
- Not edit generated files (e.g., `pdf/`, `tex/`) directly.
- Ensure the pipeline completes fully and outputs structural integrity checks.
