# docs/manuscript/ — Agent Notes

Contains only `MANUSCRIPT_STATUS.md`: an evidence-backed statement that the
program root is an umbrella (no top-level `manuscript/`); the three real
manuscripts live in each `cogsec_multiagent_<part>/manuscript/`. Do not create
stub sections at the program root — that would fork the source of truth.
