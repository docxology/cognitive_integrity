Deployment checklists backing the practical guide: `pre_deployment.py`,
`operational.py`, `incident_response.py`, `config_reference.py`, `models.py`
(shared dataclasses). The manuscript's checklists render from these modules —
edit here, not in prose, when changing checklist content.