# HANDOFF — Cognitive Security for Multiagent Operators (cognitive_integrity)

**For a new Hermes agent.** Confirm the work below, then extend it. Everything in
this file is verified/current as of 2026-08-01. Read this whole file plus the
repo `README.md`, `AGENTS.md`, and `HANDOFF_2026-07-31.md` before acting.

---

## 1. Repo & layout

- **Canonical (real, standalone git repo):** `/Users/4d/Documents/GitHub/projects/working/cognitive_integrity`
  - Remote: `https://github.com/docxology/cognitive_integrity.git` — **push here, not the template monorepo.**
  - Current HEAD: `f2bc32d`, branch `main`, clean tree, in sync with `origin/main`.
  - Also reachable via the template symlink: `/Users/4d/Documents/GitHub/template/projects/working/cognitive_integrity` (same tree).
- Three-part program **"Cognitive Security for Multiagent Operators,"** built on the **Cognitive Integrity Framework (CIF)**:
  | Part | Dir | Focus | Version | DOI | Render name |
  |------|-----|-------|---------|-----|-------------|
  | 1 | `cogsec_multiagent_1_theory` | Formal foundations (trust calculus, semiring composition, adversary taxonomy, CIF-AD/OODA, info-geometry) | **1.1** | 10.5281/zenodo.18364119 | `cognitive_integrity/cogsec_multiagent_1_theory` |
  | 2 | `cogsec_multiagent_2_computational` | Implementation + empirical validation + redteam/adversarial-training module | **1.0** | 10.5281/zenodo.18364128 | `cognitive_integrity/cogsec_multiagent_2_computational` |
  | 3+4 | `cogsec_multiagent_3_practical` | Practitioner guide + cross-domain CIF-AD-OODA applications | **1.0** | 10.5281/zenodo.18364130 | `cognitive_integrity/cogsec_multiagent_3_practical` |
- The parts are **nested** under one repo; the template render stage requires qualified names `--project cognitive_integrity/<part>` **or** the leaf-symlink workaround (see §5).

## 2. Current verified state (do not regress)

- **Versions (authoritative metadata):** Part 1 = `1.1`, Parts 2 & 3 = `1.0`; set in `manuscript/config.yaml` (`paper.version`) and `pyproject.toml` (PEP440: `1.1.0` / `1.0.0` / `1.0.0`). READMEs, manuscript version self-refs, and the Part‑3 config-version test all reconciled to these.
- **Test suites (green on HEAD):** Part 2 = **3334 passed / 5 skipped / 0 failed** (run via `cogsec_multiagent_2_computational/.venv/bin/python -m pytest tests/` or `... -m pytest -q`); Part 1 = **355 passed**; Part 3 = **882 passed** (Parts 1 & 3 via `uv run pytest tests/` from each part dir). Ruff clean on all three (`uv run ruff check src scripts tests`).
- **Claims gate:** `scripts/verify_claims.py` (Part 2) → **163 claims: 159 MATCH / 0 MISMATCH / 0 NOT_FOUND / 4 UNBACKED** (the 4 UNBACKED are env-gated LLM validation; pinned in `tests/test_claim_registry.py::KNOWN_UNRECONCILED`, which now contains exactly those 4).
- **Manuscript verifiers:** `uv run python scripts/verify_manuscript.py` passes (Files/Citations/Labels-Refs/Images/… ) in all three parts; every figure reference resolves against `output/figures/` and is git-tracked.
- **Rendering quality (done this session):**
  - PDFs open on the **cover/title page** (removed stray `\listoffigures`/`\listoftables` from each `manuscript/preamble.md`).
  - All **formalisms auto-numbered** (amsthm + section-numbered `\newtheorem`; only outlier — Part 2 monad-bind — fixed from `align*`→`align`+label). Zero unnumbered formalisms remain.
  - **Citations + cross-refs are hyperlinks** (hyperref + natbib + cleveref `nameinlink` loaded in all three combined renders; `\citep`/`\cite` link to the bibliography).
- **Figures:** every real figure is embedded with accurate captions. Part 1 = 17 refs, Part 2 = 8, Part 3 = 8; no orphaned figures remain; Part 1 quantitative figures are captioned "schematic/illustrative (not measured)"; Part 3 `config.yaml` declares exactly the 8 real figures.
- **Deliverable PDFs** (current) in `~/Downloads/`: `CogSec_Part1_Theory_v1.1.pdf` (127 pp), `CogSec_Part2_Computational_v1.0.pdf` (136 pp), `CogSec_Part3_Practical_v1.0.pdf` (106 pp).

## 3. Hard-won operational knowledge (critical)

- **Render command (template root `/Users/4d/Documents/GitHub/template`, after `uv sync`):**
  ```bash
  uv run python scripts/pipeline/stage_03_render.py --project cognitive_integrity/<part>
  ```
  BUT the intermediate symlink (`projects/working/cognitive_integrity`) trips a path guard ("project path escapes projects/"). **Workaround:** create a temporary **leaf** symlink and render that, then remove it:
  ```bash
  cd /Users/4d/Documents/GitHub/template/projects/working
  ln -sfn /Users/4d/Documents/GitHub/projects/working/cognitive_integrity/<part> ./<part>
  cd /Users/4d/Documents/GitHub/template
  uv run python scripts/pipeline/stage_03_render.py --project working/<part>   # leaf symlink allowed
  rm -f /Users/4d/Documents/GitHub/template/projects/working/<part>            # always clean up
  ```
  Rendering also builds per-section **beamer slides + HTML**; those are slow and the slide (beamer) compilations emit **"Undefined control sequence"** warnings — these affect ONLY the slide decks, NOT the combined manuscript PDF (which compiles cleanly). The combined PDF is produced regardless.
- **Environments:** Part 2 = its own `.venv` (`bfl.../cogsec_multiagent_2_computational/.venv/bin/python`, no `yaml` module — parse config textually if needed). Parts 1/3 = `uv run` from the part dir.
- **macOS ~/Downloads TCC:** `ls`/`find`/`readdir` fail ("Operation not permitted"); **`cp`/`touch`/`write` work**. Don't list Downloads — `cp` directly and verify via `pdfinfo` (page count) on the copy. Temp throwaway verify scripts should use Python `tempfile.mkstemp`/`mkdtemp` with a literal `hermes-verify-` prefix under `/private/var/folders/vc/rgmbpjpj0dbg61vr54xjskc80000gn/T`, and be removed in one place.
- **Rendered artifacts:** most `output/` is gitignored; a small allowlist of evidence files (Part 1 output/pdf + certain slides/web/data) is force-tracked. When a render changes tracked evidence, `git add -u <part>` and commit; ignore regenerated `manuscript_verification.log` (restore with `git checkout --`).

## 4. This session's completed work (summary — trust, don't redo)

1. **Red-team (adversarial) assessment** — three parallel red-team reviews (Part 1 theory soundness; Part 2 empirical/model validity + redteam/AT module + statistics; code/security/reproducibility). Consolidated in **`cogsec_multiagent_2_computational/docs/RED_TEAM_ASSESSMENT.md`** with severity + resolution status (RESOLVED / PARTIAL / OPEN). Historical audits moved to `docs/audits/` under an `audits/README.md` index.
   - **Fixed (RESOLVED):** Part 1 drafting artifacts (`Wait---`, `\\cref` LaTeX, `4ε²/1` typo), drift-threshold contradiction (0.775 rad/25%), honest model-checking table ("deferred to Part 2"), independence-assumption + semiring-scope caveats added; Part 1 quantitative figure captions relabeled illustrative; regenerated `statistical_results.json` with honest provenance (simulated-control note) while preserving the `real_pipeline` preservation marker; null timestamps in colony/scalability/detection artifacts (byte-for-byte determinism); Part 2 §05g AT results were already honest at the prose level and the design-model scope + real-mode no-op is now disclosed; AT Key-Findings/Nash reframed as "by construction of the closed-form design model."
   - **OPEN (recorded for the next author/agent):** Part 1 H1 independence full re-derivation vs. union bounds, H2 semiring vs. "bounded distributive lattice" (caveat added, full re-proof open), H3 Fisher–Rao `I·S≤π/2` (artifact removed, claim still needs author decision), H5 KL–AUC bound correction, H7 blast-radius reachable-factor alignment; Part 2 M3 t-CI vs z-CI, real-mode AT threshold coupling, M4 AT batch de-dup, statisticalResults effect sizes (d≈62) must NOT be reported as real; LOW-3 redteam `evasion_score` rename to `heuristic_evasion_score`.
2. **Re-versioning** — Part 1 → 1.1, Parts 2/3 → 1.0 across config.yaml, pyproject, READMEs, manuscript self-refs, and the Part 3 config-version test.
3. **Figures/manuscript completeness** — wired every real figure in with accurate captions (Part 1: +6; Part 2: +1 trust-decay; Part 3: 8), removed stale artifacts, force-tracked referenced-but-untracked figure assets, updated all docs indexes.
4. **PDF rendering + delivery** — re-rendered all three; cover-page-first; formalisms auto-numbered; citations/cross-refs hyperlinked; delivered to `~/Downloads/` with the versioned names above.

## 5. Natural next steps / how to extend

1. **(Recommended first) Fix the beamer slide "Undefined control sequence" errors** — the only remaining render blemish. Inspect the slide-renderer template + the LaTeX commands the manuscripts inject into beamer (`\cogstate{}`, `\cref`/`\Cref` in slides, stmaryrd/tikz-cd macros, etc.) and reconcile the beamer preamble so all per-section slide decks compile exit-code 0. Re-render the affected parts and re-deliver.
2. **Close the OPEN red-team items** (see `docs/RED_TEAM_ASSESSMENT.md`): highest-value are Part 1 H1 independence (union/Fréchet bounds or explicit `ρ`) and H5 KL–AUC bound + recomputed table; Part 2 real-mode AT threshold coupling; statistical effect-size disclosure. Update the assessment doc's status columns as you resolve them.
3. **Publication hardening before upload:** ensure Zenodo metadata matches config (title, authors, version, DOI-reserved status); consider `CITATION.cff`; confirm each manuscript's `references.bib` compiles (no missing keys); decide slide-deck inclusion.
4. **Determinism/evidence hygiene:** any new committed artifact must carry the provenance keys and no wall-clock timestamps; never stamp `data_origin: real_pipeline` on closed-form design models; always run the claims gate + full affected suites before committing.

## 6. Guardrails (non-negotiable)

- **No mocks** (`MagicMock`/`mocker`/`mock.patch`) — use real data/computation/HTTP; tests validate behavior.
- **Honest provenance:** manuscript numbers must match reproducible data; never fabricate; label schematic/illustrative vs measured; mark closed-form as such.
- **Push only to `docxology/cognitive_integrity`** (main); the `projects/working/` tree stays untracked in the template monorepo.
- **Verification before claiming done:** run the affected suite(s), `scripts/verify_claims.py` (Part 2), `scripts/verify_manuscript.py` (each part), and ruff; then commit in logical slices and push.
- Ad-hoc verification scripts: `hermes-verify-`-prefixed under the temp root, one cleanup path.
