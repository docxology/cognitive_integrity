You are picking up work on the Cognitive Integrity Program — a three-part manuscript series on cognitive security for multiagent AI operators. The prior session (Hermes Agent + deepseek-v4-pro) completed a comprehensive review-and-fix pass across all three papers. Your job: verify the work, fix remaining test failures, and extend with any remaining improvements.

## REPOSITORY

Primary: /Users/4d/Documents/GitHub/template/projects/working/cognitive_integrity
It's a standalone git repo at branch `main`, symlinked into the template.
HEAD: 967b1e9 (checkpoint: prior uncommitted passes + Part 2 correctness wave 1)
95 uncommitted changes in the working tree (mostly from prior audit fixes + this session's work)

Three papers inside:
- cogsec_multiagent_1_theory/ — Part 1 v2.0: Formal Foundations (DOI: 10.5281/zenodo.18364119)
- cogsec_multiagent_2_computational/ — Part 2 v2: Computational Validation (DOI: 10.5281/zenodo.18364128)
- cogsec_multiagent_3_practical/ — Part 3+4 merged: Practical Guide + Applications (DOI: 10.5281/zenodo.18364130)

## WHAT WAS DONE IN THE PRIOR SESSION

### Cross-paper fixes (5 files):
- Part 1 conclusion: "Part 3 offers" → "Part 3+4 offers", "Part 3: Practitioner Guidance" → "Part 3+4"
- Part 1 introduction: "Part 3: Practitioner Guidance" → "Part 3+4", "merged Part 3" → "merged Part 3+4", "consult Part 3" → "consult Part 3+4"
- Test counts updated: 2,283 → 3,308 in Part 2 src/AGENTS.md, Part 3 abstract, Part 3 introduction
- Part 3 abstract: "(Part 3) combining" → "(Part 3+4) combining"

### Research integrity fixes (manuscript tables rewritten with real generated data):
- 03_attack_corpus.md: Removed fabricated dataset-source tables (fake JailbreakBench/PromptInject/TensorTrust citations). Replaced with real generator difficulty distribution + provenance warning.
- 05d_ablation_and_scalability.md: Complete rewrite — all scaling, regression, and ablation tables now sourced from real generated data. Key corrected values: trust_calculus ΔTPR = -0.020 (was -0.007 before bug fix), scaling n=100 memory = 873MB (was 1.6GB), regression R² = 0.9998 (was 0.994).
- 05g_adversarial_training.md: Table replaced with real values from run_adversarial_training.py. Base DR rises (not falls), Nash = 1.0 (was 0.505).
- 05h_redteam_evaluation.md: Table replaced with real values (all 0.000 evasion). Known limitations section added.
- 05b_statistical_significance.md: Component and synergy tables replaced with real ablation data.
- 05f_architecture_gap_analysis.md: All deltas corrected. Trust Calculus elevated with note about the key-mapping fix.
- 04_experimental_setup.md: Ablation corpus 100→98 attacks, TPR 0.124→0.122.

### Real data generated:
All real generator scripts were run at seed 42, producing fresh output in cogsec_multiagent_2_computational/output/data/:
- ablation_results.json, scalability_data.json, redteam_evaluation_results.json
- adversarial_training_results.json, full_evaluation_results.json, cross_validation_results.json
- statistical_results.json, multi_seed_results.json, colony_results.json

### Auto-injection:
The manuscript injector (src/manuscript/injector.py) was run, applying 34 numeric substitutions across 7 manuscript files. Values now sourced from real generated data. 6 LLM validation claims correctly unbacked (Ollama not available).

### Code quality:
- Part 3 checklists: 5 ruff auto-fixes (unused imports in incident_response.py, operational.py; import sorting in pre_deployment.py)

## CURRENT GATE STATUS

| Gate | Part 1 | Part 2 | Part 3 |
|------|--------|--------|--------|
| Tests | 355 passed | 3,303 passed, 5 failed, 5 skipped | 873 passed |
| Coverage | 96.42% | 97.13% | 94.72% |
| Ruff | 0 violations | 0 violations | 0 violations |

## KNOWN FAILURES TO FIX

In cogsec_multiagent_2_computational/:

1. tests/test_manuscript_injector.py::test_every_substitution_matches_the_real_manuscript
   → 5 injector regex patterns no longer match because the manuscript prose was rewritten.
   These patterns in src/manuscript/injector.py need updating:
   - "component_order_caption" (line ~826): expects "followed by .+?\\. The" — the rewritten 05d doesn't use that phrasing
   - "top_synergy_value" (line ~315): expects "strongest positive synergy" — 05d now uses tier-based ranking
   - "top_synergy_pair" (line ~855): expects "The [A-Za-z]... pairs? exhibits? the strongest" — 05d uses different structure
   - "component_hierarchy" (line ~883): expects "Detection module\\s*\\$[>=]\\$[^.]+\." — 05d has different hierarchy format
   - "full_pipeline_tpr_prose" (line ~1137): expects "(full pipeline \\$\\s*(?:=|\\\\approx|\\\\sim)?\\s*)[\\d.]+" — 05b now uses different phrasing

2. tests/test_manuscript_injector.py::test_the_real_manuscript_audit_can_actually_fail
   → 6 LLM claims correctly unbacked (llm_demo_results.json status=skipped, Ollama not available).
   This test should be updated to expect the 5 pattern failures above PLUS the 6 LLM claims.

3. tests/test_script_entry_points.py: 2 other failures (check exact names — may have shifted with changes).

## REMAINING WORK (from AUDIT_2026-07-26.md — 114 issues, 13 CRITICAL)

The AUDIT is at: cogsec_multiagent_2_computational/docs/AUDIT_2026-07-26.md

### CRITICAL — still need human judgment or new experiments:
- MSC-01 (79 '??' in PDF): All table labels use bold paragraph format `{tab:xxx}` which pandoc-crossref doesn't resolve. Fix: convert to proper markdown table syntax or LaTeX table environments. Or build a post-processing script that inserts \label for each {tab:xxx} attribute. Current workaround: replaced one \cref{tab:redteam-omega} with plain text.
- MISS-01 (no reader-side verifier): claim_registry.py is already written at src/manuscript/claim_registry.py. Wire it into CI. Run `uv run python scripts/verify_claims.py` to see current 58 unverified claims. Fix tolerance values in claim_registry.py for the minor mismatches (e.g., 0.122 vs 0.122449), then add a CI step.
- MISS-02 (no baseline comparator): baselines.py exists at src/evaluation/baselines.py with 1,393 lines. tests/test_baselines.py has 1,070 lines. Wire the baseline comparison into the manuscript (add a new subsection in 05_results.md).

### HIGH — structural manuscript fixes:
- MSC-03 (adversarial training): Table is updated with real values but the convergence analysis still uses narrative language that may not match. The "approximately linear rather than geometrically decaying" language I added should be reviewed.
- MSC-02 (redteam): The mutation-operator table now shows real zeros. The Ω-level coverage table at the bottom of 05h is currently illustrative — regenerate from real data if possible.
- INTEG-02 (LLM injector): Now fails-closed (returns None instead of hardcoded defaults). But the manuscript still contains LLM validation prose with now-unbacked claims. Either run LLM validation (needs Ollama + COGSEC_RUN_LLM_ANALYSIS=1) or restructure those sections to note LLM validation is pending.

### Candidate for next: fix the injector patterns so all tests pass
The injector test failures are the most actionable next step. Fixing them means:
1. Read the 5 unmatched patterns in src/manuscript/injector.py
2. Read the corresponding manuscript sections I rewrote
3. Update each regex pattern to match the new prose
4. Re-run `uv run pytest tests/test_manuscript_injector.py -v` to confirm

## VERIFICATION COMMANDS (paste these to confirm state)

```bash
cd /Users/4d/Documents/GitHub/template/projects/working/cognitive_integrity

# 1. Git state
git log --oneline -3
git status --short | wc -l  # should be ~95

# 2. Ruff (should be "All checks passed!")
ruff check cogsec_multiagent_1_theory/src/ cogsec_multiagent_2_computational/src/ cogsec_multiagent_3_practical/src/

# 3. Part 1 tests (should be 355 passed)
cd cogsec_multiagent_1_theory && uv run pytest tests/ -q && cd ..

# 4. Part 2 tests (should be ~3300 passed, few failures)
cd cogsec_multiagent_2_computational && uv run pytest tests/ -q --tb=line 2>&1 | tail -3 && cd ..

# 5. Part 3 tests (should be 873 passed)
cd cogsec_multiagent_3_practical && uv run pytest tests/ -q && cd ..

# 6. Verify cross-references are clean
grep -rn "Part 3[^+].*Practitioner" cogsec_multiagent_1_theory/manuscript/09_conclusion.md || echo "09_conclusion: clean"
grep -rn "Part 3[^+].*Practitioner" cogsec_multiagent_1_theory/manuscript/02_introduction.md || echo "02_introduction: clean"
grep -rn "2,283" cogsec_multiagent_2_computational/src/AGENTS.md cogsec_multiagent_3_practical/manuscript/00_abstract.md cogsec_multiagent_3_practical/manuscript/01_introduction.md || echo "test counts: clean"

# 7. Check real data exists
ls cogsec_multiagent_2_computational/output/data/ablation_results.json \
   cogsec_multiagent_2_computational/output/data/scalability_data.json \
   cogsec_multiagent_2_computational/output/data/adversarial_training_results.json \
   cogsec_multiagent_2_computational/output/data/redteam_evaluation_results.json \
   && echo "all real data present"

# 8. Run injector (should show 34 substitutions, 0 unmatched patterns after you fix them)
cd cogsec_multiagent_2_computational && uv run python -c "
from pathlib import Path
from src.manuscript.injector import inject_all
inject_all(Path('output/data'), Path('manuscript'), dry_run=True, strict=False)
"

# 9. Verify claims
uv run python scripts/verify_claims.py 2>&1 | tail -5

# 10. Re-run real generators to confirm reproducibility
uv run python scripts/run_ablation.py --seed 42 2>&1 | tail -2
uv run python scripts/run_adversarial_training.py --seed 42 2>&1 | tail -2
uv run python scripts/run_redteam.py --seed 42 2>&1 | tail -2
```

## KEY FILES CHANGED THIS SESSION

Manuscript (Part 1):
- cogsec_multiagent_1_theory/manuscript/02_introduction.md
- cogsec_multiagent_1_theory/manuscript/09_conclusion.md

Manuscript (Part 2):
- cogsec_multiagent_2_computational/manuscript/00_abstract.md
- cogsec_multiagent_2_computational/manuscript/03_attack_corpus.md
- cogsec_multiagent_2_computational/manuscript/04_experimental_setup.md
- cogsec_multiagent_2_computational/manuscript/05b_statistical_significance.md
- cogsec_multiagent_2_computational/manuscript/05d_ablation_and_scalability.md
- cogsec_multiagent_2_computational/manuscript/05f_architecture_gap_analysis.md
- cogsec_multiagent_2_computational/manuscript/05g_adversarial_training.md
- cogsec_multiagent_2_computational/manuscript/05h_redteam_evaluation.md
- cogsec_multiagent_2_computational/manuscript/05_results.md
- cogsec_multiagent_2_computational/manuscript/06_discussion.md
- cogsec_multiagent_2_computational/manuscript/07_conclusion.md

Code (Part 2):
- cogsec_multiagent_2_computational/src/AGENTS.md

Manuscript (Part 3):
- cogsec_multiagent_3_practical/manuscript/00_abstract.md
- cogsec_multiagent_3_practical/manuscript/01_introduction.md

Code (Part 3):
- cogsec_multiagent_3_practical/src/checklists/incident_response.py
- cogsec_multiagent_3_practical/src/checklists/operational.py
- cogsec_multiagent_3_practical/src/checklists/pre_deployment.py

## PRINCIPLES TO FOLLOW

- Never invent numbers. Every value in the manuscript must be traceable to a real data file.
- The injector is the ground-truth pipeline: run it after every manuscript edit.
- The claim verifier is the gate: `uv run python scripts/verify_claims.py` must pass before declaring done.
- All three papers must stay green: ruff 0 violations, all tests pass.
- If you regenerate data (e.g., change a script), re-run the injector and re-verify claims.
- Before committing: review the 95-file diff. Consider committing in slices: cross-paper fixes first, then Part 2 manuscript, then Part 2 code, then Part 3.
