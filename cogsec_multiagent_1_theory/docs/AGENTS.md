# cogsec_multiagent_1_theory/docs/ — Agent Notes

Part 1 documentation: `README.md` — the formal-foundations documentation index
(manuscript map, figures, tests). Keep it in sync with the manuscript sections
and `src/` modules; claims shared with Parts 2/3 are checked by
`../../scripts/check_series_integrity.py`.
